//implement a user  in ts to make sure that the data we get back from the API is in the correct format and has the correct properties.
//
export interface User {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}


